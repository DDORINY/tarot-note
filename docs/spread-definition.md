# Spread Definition

스프레드는 다음 필드를 가집니다.

- `id`
- `name`
- `description`
- `cardCount`
- `category`
- `isPremium`
- `positions`

포지션은 `index`, `label`, `meaning`, `x`, `y`, `rotation`, `zIndex`로 구성됩니다. `x`, `y`는 `SpreadBoard`에서 퍼센트 좌표로 렌더링됩니다.
